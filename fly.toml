# fly.toml — Autoresponder WhatsApp 2025

app = "autoresponder-wa"
primary_region = "mia"

[build]
  dockerfile = "./Dockerfile"

# 👇 Fly.io usará tu GITHUB_TOKEN para hacer push
[env]
  NODE_ENV = "production"
  GIT_REPO = "${GITHUB_REPO}"
  GIT_TOKEN = "${GITHUB_TOKEN}"

[deploy]
  strategy = "immediate"

[[statics]]
  guest_path = "/app/public"
  url_prefix = "/"
